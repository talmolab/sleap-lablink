# LabLink Infrastructure Environment Variables
# Copy this file to .env and fill in your actual values
#
# NOTE: Most configuration is now in config.yaml
# This file is only needed for local Terraform deployments

# AWS Credentials (for local Terraform deployments)
# Not needed if using AWS CLI configured credentials
# Not needed for GitHub Actions (uses OIDC)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_DEFAULT_REGION=us-west-2

# GitHub Container Registry (only if using private Docker images)
# GITHUB_TOKEN=your_github_personal_access_token
